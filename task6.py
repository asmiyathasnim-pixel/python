# Fixed Values Program 
# Write a program that works with fixed values that do not change. 
# The program should separate the values, join them with other fixed values, and print the 
# result. 


fnum = input('Enter your first name: ')
snum = input('Enter your second name: ')
age = int(input('Enter your age: '))

name = fnum +  snum
details = (f'I am {name}. I am {age} years old.')

print(details)

